#include <stdio.h>
#include <string.h>

int main()
{

    char a[] = " bro";
    char b[] = " partha";
    // strlwr(a);
    // strupr(a);
    // strcat(a,b);
    // strcpy(a,b);

    printf("= % s", a);

    return 0;
}